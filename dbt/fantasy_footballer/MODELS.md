# dbt data-model map

A human- and agent-readable map of every model in this dbt project: each model's
**grain**, **purpose**, **key columns**, and **upstream** refs. Use it to understand the
warehouse without re-reading every `.sql` file.

> **Maintenance:** this file is a living map. **If you add, remove, or materially change a
> model (grain, purpose, key columns, or its upstream refs), update the corresponding entry
> here in the same change.** Per-column type/description detail lives in each directory's
> `properties.yml` (contract-enforced) — don't duplicate it here; this file is the
> cross-model overview that `properties.yml` can't express.

To inspect any model's actual data, use the read-only helper (see root `CLAUDE.md`):
`make query SQL="select * from main_intermediate.int__owner_season_scoring limit 10"`.

---

## Conventions you need to read this map

- **Schemas (DuckDB catalog `main`):** `main_base`, `main_staging`, `main_intermediate`,
  `main_marts`, `main_seed_data`.
- **Composite IDs** are the standard join keys, built with `||`:
  | ID | Format | Grain it identifies |
  |---|---|---|
  | `team_year_id` | `team_id_year` | a team in a season |
  | `team_week_id` | `team_id_year_week` | a team in one week |
  | `owner_year_id` | `owner_id_year` | an owner in a season |
  | `player_year_id` | `playerid_year` | a player in a season |
  | `player_week_id` | `playerid_year_week` | a player in one week |
- **`outcome` codes** (on weekly grains): `W` win, `L` loss, `T` tie, `U` unplayed/future.
  Almost every "played games" filter is `outcome != 'U'`.
- **`meta__source_path` / `meta__date_effective` / `meta__date_pulled`** ride through every
  `base_*` model unchanged; they power `stg__source_metadata` → `int__source_freshness`.
- **Owner identity** (`owner_name`) is *not* in the source — it's attached by joining
  `int__owner_team_year_map` (the bridge from ESPN `display_name` → seed `owner_id`).

## Lineage at a glance

```
s001 sources ─► base_s001__*  ─►  staging (stg__*)  ─►  intermediate (int__*)  ─►  marts/<page>
seeds (owner_names,            (unnest arrays,        (reusable computations)     (page-specific,
 display_names, users)          flatten to weekly                                  owner_name attached,
                                grain)                                             rounded for display)
```

---

## Base layer (`main_base`) — 1:1 with sources, cast + rename only

| Model | Grain | Purpose & key columns | Upstream |
|---|---|---|---|
| `base_s001__teams` | team-season (`team_year_id`) | Season team record (`wins/losses/ties`, `points_for/against`, `standing`, `final_standing`, `streak`, `playoff_pct`). Holds **`weeks_raw`** — a struct array, one entry per matchup period `{week, lineups, score_for, outcome, opponent_team_id}`, where `lineups` is the roster captured **per NFL week** (two NFL weeks in the 2018-2019 playoffs); staging unnests it. Derives `display_name` from `owners[1]` first/last name (the link to owner identity). | `source(s001, teams)` |
| `base_s001__matchups` | team-matchup_week (home+away unioned) | One row per team per matchup. `score_for`, `projected_score_for`, `matchup_lineup`, `is_playoff`, **`matchup_type`**. Used for playoff classification + matchup lineups. | `source(s001, matchups)` |
| `base_s001__players` | player-season (`player_year_id`) | Player meta (`position_slot`, `is_flex`, `nfl_team`, ranks, ownership). Holds **`stats_raw`** — weekly stats array that `stg__player_weeks` unnests. | `source(s001, players)` |
| `base_s001__settings` | season (`year`) | League config: `reg_season_count`, `playoff_team_count`, `team_count`, **`matchup_weeks`** (matchup_period→weeks map), `scoring_format`, tie rules. | `source(s001, settings)` |
| `base_s001__draftpicks` | pick (`team_year_id` + `player_year_id`) | Draft results: `round_num`, `round_pick`, `bid_amount`, `keeper_status`, `is_auction` (= `nominating_team_id is not null`), `nominating_team_year_id`. | `source(s001, draftpicks)` |

## Seeds (`main_seed_data`) — populated from B2 at boot, git-ignored

| Seed | Grain | Purpose |
|---|---|---|
| `owner_names` | `owner_id` | Canonical owner identity (`owner_id` → `owner_name`). |
| `display_names` | `owner_id` | ESPN `display_name` (first+last) → `owner_id`. Bridges source names to identity. |
| `users` | username | Auth (login hashes). Not part of analytics lineage. |

## Staging (`main_staging`) — unnest arrays, flatten to weekly grain

| Model | Grain | Purpose & key columns | Upstream |
|---|---|---|---|
| `stg__team_weeks` | **team-matchup-week** (`team_week_id`) | **The weekly workhorse.** Unnests `teams.weeks_raw` to one row per team per matchup period: `score_for`, `outcome`, `lineups` (per-NFL-week rosters), and opponent keys (`opponent_team_week_id` for self-joins). Self-join by role (`team_weeks` + `opponent_weeks`) for head-to-head. | `base_s001__teams` |
| `stg__team_week_players` | team-(NFL week)-player (`player_week_id`) | Unnests `stg__team_weeks.lineups` (the per-NFL-week rosters) → each rostered player's slot **per NFL week**, carrying `matchup_week` (scoring period) + `week` (NFL week). The 2018-2019 two-week playoff matchups yield two NFL-week rows per player; everything else is 1:1. `week`/ids are NFL-week-based so they pair 1:1 with `stg__player_weeks`. `RB/WR/TE` slot normalized to `FLEX`. | `stg__team_weeks` |
| `stg__player_weeks` | player-week (`player_week_id`) | Unnests `players.stats_raw` → weekly player box score (`points`, passing/rushing/receiving/kicking stat lines, `team_win`). | `base_s001__players` |
| `stg__matchup_players` | matchup-week-player | Unnests `matchups.matchup_lineup` (`score_for > 0`, i.e. real weeks). Carries `is_playoff`, `matchup_type`. | `base_s001__matchups` |
| `stg__owner_display_names` | `display_name` | Joins `owner_names` + `display_names` seeds → `owner_id` / `owner_name` / `display_name` bridge. | seeds |
| `stg__settings_matchup_weeks_map` | year-matchup_week-week | Unnest + unpivot `settings.matchup_weeks` → maps a matchup_week to its calendar week(s). | `base_s001__settings` |
| `stg__source_metadata` | src_table-year | Unions `meta__*` columns across base models for freshness tracking. | base models |

## Intermediate (`main_intermediate`) — reusable computations

| Model | Grain | Purpose & key columns | Upstream |
|---|---|---|---|
| `int__owner_team_year_map` | team-season (`team_year_id` unique) | **The owner-identity bridge.** Maps `owner_id`/`owner_name`/`owner_year_id` ↔ `team_id`/`team_year_id`/`team_name`/`year` (via `display_name`). Joined by nearly every mart to surface `owner_name`. | `base_s001__teams`, `stg__owner_display_names` |
| `int__owner_team_map` | owner-team | Dedup owner↔team across seasons (franchise ownership history, year-agnostic). | `int__owner_team_year_map` |
| `int__current_season_year` | single row | Latest season **that has been drafted** (max year in draft picks) as `current_season_year` — the fantasy year starts at the draft, and the upcoming season is an all-zero offseason shell until then, so plain `max(year)` is wrong. Cross-join into any mart that should auto-update each season. **Never hard-code the year.** | `base_s001__draftpicks` |
| `int__matchup_week_playoff_map` | year-week | `is_playoff` flag per calendar week. | `stg__settings_matchup_weeks_map`, `base_s001__matchups` |
| `int__weeks_played_by_year` | year | Min regular-season weeks played that season. | `stg__team_weeks`, `int__matchup_week_playoff_map` |
| `int__clutch_records` | team-season (`team_year_id` unique) | Clutch W-L `record` string (regular-season games decided by `< 10` pts), derived from the `is_clutch` flag. Season points for/against moved to `int__owner_season_scoring`. | `int__team_week_results` |
| `int__shotguns` | team-week | `is_shotgun` = `score_for < 100` **or** the week's league-minimum score (regular season). | `stg__team_weeks`, `int__matchup_week_playoff_map` |
| `int__strength_of_schedule` | team-season (`team_year_id` unique) | Opponent-Wins (`ow`), Opponent's-Opponent-Wins (`oow`), composite `sos = ⅔·ow + ⅓·oow`; `*r` variants = remaining games. | `stg__team_weeks`, `base_s001__teams`, `base_s001__settings` |
| `int__source_freshness` | src_table (current season) | Current-season slice of `stg__source_metadata` for freshness display. | `stg__source_metadata`, `int__current_season_year` |
| `int__owner_season_scoring` | owner-season (`owner_year_id` unique) | **The canonical season-scoring model** (points for AND against): `reg_points_total`, `reg_points_against`, `reg_points_per_game`, `games_played`, `best_week_score`, `worst_week_score`, `made_playoffs`. Non-clutch marts read season points from here. | `int__team_week_results`, `int__owner_team_year_map`, `int__team_postseason` |
| `int__postseason_team_weeks` | team-week (postseason only, `team_week_id` unique) | Reconstructs bracket progression. Per postseason game: `bracket`, `matchup_type`, `is_championship_game`, `is_third_place_game`, `is_toilet_game`, and **`is_meaningful`** (does the game count toward metrics). See Postseason gotcha below. | `stg__team_weeks`, `base_s001__teams`, `base_s001__settings`, `stg__settings_matchup_weeks_map`, `base_s001__matchups` |
| `int__team_postseason` | team-season (`team_year_id` unique) | End-of-regular-season status + final placement: `bracket` ∈ {winners, toilet_bowl, kiss_my_sister}, `made_playoffs`, placement flags (`is_champion`, `is_runner_up`, `is_third`, `is_second_to_last`, `is_last`), `reconstructed_place`, plus ESPN `final_standing` (validated against the reconstruction for places 1-3). | `int__postseason_team_weeks`, `base_s001__teams` |
| `int__matchup_margins` | regular-season game (`team_week_id` unique) | One row per head-to-head game: `winner_team_year_id`, `loser_team_year_id`, scores, `margin`, `combined` (= winner+loser score, for shootout/slugfest records), `is_tie`. Feeds tightest-matchups / biggest-blowouts and highest-shootouts / lowest-slugfests. A singular test (`assert_matchup_margins_arithmetic`) pins `margin`/`combined` to winner∓loser score. | `stg__team_weeks`, `int__matchup_week_playoff_map` |
| `int__lucky_records` | owner-week (regular season, `team_week_id` unique) | Median-based luck (definition per the alexcates.com "better to be lucky than good" article): `is_lucky_win` (won below week median), `is_unlucky_loss` (lost above it), and experimental `luck_points` (teams above on a win / below on a loss). | `stg__team_weeks`, `int__matchup_week_playoff_map` |
| `int__league_season_week_extremes` | season (`year` unique) | One row per season: the league-wide single-week extremes over played reg-season weeks — `league_best_score`/`league_worst_score` (Best-/Worst-week titles) and `tightest_margin`/`biggest_margin`. **Single source of truth** consumed by both `int__season_titles` (matchup titles) and `int__team_week_highlights` (chips), so the two can't drift (FF-011). A singular test asserts the title↔chip alignment. | `int__team_week_results` |
| `int__season_titles` | owner-season (`owner_year_id` unique) | Title roll-up: amount columns + a boolean flag per season title (scoring/non-scoring, matchup, snub, playoff luck-in, clutch win/lose, lucky/unlucky, shotgun, no-shotgun). Co-title semantics (rank()=1 for points/clutch/luck/shotgun; **matchup titles** instead compare each team's `best_week_score`/`worst_week_score` to the shared `int__league_season_week_extremes`, so they can never drift from the `is_best_week`/`is_worst_week` chips). **Snub/luck-in are gated:** a self-join (`points_vs_field`) counts `playoff_teams_outscored` / `nonplayoff_teams_outscoring`, and snub requires outscoring ≥1 playoff team (luck-in the mirror) — so clean-cutoff years award neither. Those counts also size the season-card subtitle ("outscored 3 who made it"). Beyond the single-title flags, `is_snubbed` / `is_lucked_in` mark **every** snubbed/lucked-in team-season (the occurrence, not just the worst) — feeding the all-time career-total + biggest-snub leaderboards. Also carries `points_left_on_table`/`lineup_efficiency` + **two complementary lineup-setter title pairs** (from `int__owner_lineup_efficiency`): `is_best_lineup_title`/`is_worst_lineup_title` (fewest/most reg-season points left on the bench vs optimal — absolute production) and `is_most_efficient_lineup_title`/`is_least_efficient_lineup_title` (highest/lowest share of the optimal captured — scoring-normalized, so a low-scoring team that set near-perfect lineups can still lead). | `int__owner_season_scoring`, `int__clutch_records`, `int__shotguns`, `int__lucky_records`, `int__league_season_week_extremes`, `int__owner_lineup_efficiency` |
| `int__season_titles_long` | owner-season × title metric | **Tidy/unpivoted `int__season_titles`** — the single place the title → `amount` → `is_title_holder` mapping is defined. Every owner-season fans out to one row per title metric (incl. `best_lineup_title`/`worst_lineup_title`, amount = `points_left_on_table`, and `most_efficient_lineup_title`/`least_efficient_lineup_title`, amount = `lineup_efficiency`×100). Consumed by `season_highlights` (holders) and `int__owner_career_summary` (counts/sums). | `int__season_titles` |
| `int__optimal_lineup_players` | team-(NFL week)-player (regular season **+ meaningful postseason games**, `player_week_id` unique) | Per rostered player: `is_started` + `is_optimal` (belongs in the best legal lineup that NFL week) with `actual_slot`/`optimal_slot`, `matchup_week`, `week` (NFL) + `is_playoff`. Greedy: top scorers per position fill dedicated slots, then FLEX from the best remaining RB/WR/TE. Rosters arrive **per NFL week** from `stg__team_week_players`, so points pair 1:1 with `stg__player_weeks` and the 2018-2019 two-week playoff matchups yield both NFL-week lineups (reconciling to the matchup total). Postseason weeks are limited to meaningful bracket games (via `int__postseason_team_weeks`, matched on `matchup_week`) so the spotlight Roster view can show them; `is_playoff` rows are excluded from the season aggregate. Building block for the team-week totals, the weekly Roster view, and the underutilized-player metric. | `stg__team_week_players`, `base_s001__players`, `stg__player_weeks`, `int__matchup_week_playoff_map`, `int__postseason_team_weeks` |
| `int__optimal_lineups` | team-week (regular season, `team_week_id` unique) | Thin aggregation of `int__optimal_lineup_players` **filtered to `not is_playoff`**: `actual_points` (started) vs `optimal_points` (best legal lineup; `greatest(optimal, actual)` guards edge cases). The gap = depth-neutral avoidable start/sit loss. | `int__optimal_lineup_players` |
| `int__owner_lineup_efficiency` | owner-season (`owner_year_id` unique; carries `team_year_id`) | Season rollup of `int__optimal_lineups`: `points_left_on_table` (optimal − actual) + `lineup_efficiency` (actual / optimal). Backs the lineup-setter titles (both the points-left and efficiency pairs). | `int__optimal_lineups`, `int__owner_team_year_map` |
| `int__owner_player_underutilization` | owner-season-player (`owner_player_year_id` unique) | One row per player who left points on the bench: `points_left_on_bench` = reg-season points the player scored in weeks they belonged in the owner's optimal lineup but were benched (`is_optimal and not is_started`), with `weeks_benched_deserving`. The player-level decomposition of start/sit loss — credits a benched player only for weeks starting them would have helped. Backs the **underutilized-player** highlight (By-Season title + all-time record). | `int__optimal_lineup_players`, `int__owner_team_year_map` |
| `int__owner_career_summary` | owner (`owner_id` unique) | Career roll-ups per owner: tennis-style title counts (incl. `best_lineup_title_count`/`worst_lineup_title_count` + `most_efficient_lineup_title_count`/`least_efficient_lineup_title_count`), cumulative lucky/unlucky/shotgun + clutch W-L totals, `seasons_played` (tenure), `snub_total`/`luck_in_total` (career snubbed/lucked-in seasons), `points_total`/`games_total`/`points_per_game`/`clutch_win_pct`, and `acquisitions_total`/`trades_total` (career roster moves, summed from `base_s001__teams` via `int__owner_team_year_map`). Keeps `all_time_records` a thin select. | `int__season_titles_long`, `int__owner_season_scoring`, `int__season_titles`, `base_s001__teams`, `int__owner_team_year_map` |
| `int__owner_postseason_summary` | owner (`owner_id` unique) | Career postseason roll-up: `playoff_appearances`, `championships`/`runner_ups`/`third_places`, `toilet_bowl_appearances`, `last_place_finishes` (dead-last = toilet bowl winner)/`second_to_last_finishes`, `best_finish` (min `final_standing`). **Game-level records are kept SEPARATE by bracket:** `playoff_wins`/`playoff_losses` = the championship (winners) bracket only (the true playoff record — reconciles with `playoff_appearances`), `toilet_bowl_wins`/`toilet_bowl_losses` = the toilet bowl only. Both count only **meaningful** games. Aggregates team-season placements + postseason game outcomes up to the owner. | `int__team_postseason`, `int__postseason_team_weeks`, `int__owner_team_year_map` |
| `int__owner_head_to_head` | owner pair (`owner_opponent_id` unique) | All-time rivalry between two owners. **Reg season and playoffs are completely separate** — every comparison metric is **regular-season only**, and playoffs surface solely as the `playoff_wins/losses/ties/games` record — **championship (winners) bracket meetings only** (toilet-bowl / consolation meetings are excluded, so it's a true playoff rivalry record), and this avoids the league's 2-week-aggregate playoff scores distorting the numbers; see CLAUDE.md. Carries: reg record + `reg_points_for/against`, reg averages (`avg_points_for`, `avg_margin`), single-game extremes as **owner-perspective** score + margin/combined + year/week context (biggest win, closest game, highest shootout, lowest slugfest — most carrying a winner `_leader` id — via `arg_max`/`arg_min`), reg-season streaks (current + longest win via gaps-and-islands), owner-perspective `last_meeting_context`, clutch-meeting record, `held_under_100`, per-opponent `lucky_wins`/`unlucky_losses` (from `int__lucky_records` on `team_week_id`), and a **separate `toilet_wins/losses/ties/games`** record (toilet-bowl meetings only — the mirror of the championship-only playoff record). Summed across opponents the reg record yields each owner's career W-L-T (a singular test, `assert_career_record_matches_source`, anchors that sum to the raw ESPN `base_s001__teams` record). A singular test bounds the extremes by the league-wide records (`int__matchup_margins`). Backs the H2H dashboard. | `int__team_week_results`, `int__lucky_records` |
| `int__team_shotgun_counts` | team-season (`team_year_id` unique) | Regular-season `shotgun_count` + sorted `shotgun_weeks` list per team-season (count 0 if none). Centralizes the tally summed inline by `current_shotgun_counter` + `season_overview`. | `int__shotguns` |
| `int__player_season_stats` | player-season (`player_year_id` unique) | Per player-season `total_points` (all weeks, reg + playoff), `reg_season_points` (regular-season weeks only — joins `int__matchup_week_playoff_map`, excludes playoff + post-season NFL weeks), `num_weeks` (week 0 excluded), plus position/flex/rank/team. Backs `player_data_table` (total) and the roster-production utilization denominator (reg). | `stg__player_weeks`, `int__matchup_week_playoff_map` |
| `int__owner_player_season` | owner-season-player (`owner_player_year_id` unique) | How each player a manager rostered produced **while on that manager's roster**, regular season only: `weeks_held`/`points_held` (every rostered week, bench/IR included) + `weeks_started`/`points_started` (active lineup slots only). A mid-season trade fans the player out to one row per holder, attributing weeks/points to each. Roster truth from `stg__team_week_players`; weekly points left-joined from `stg__player_weeks` (coalesced to 0 for bye/no-stat weeks); owner via `int__owner_team_year_map`; reg-season filter via `int__matchup_week_playoff_map`. Backs `owner_roster_production`. | `stg__team_week_players`, `stg__player_weeks`, `base_s001__players`, `int__owner_team_year_map`, `int__matchup_week_playoff_map` |
| `int__team_week_results` | team-week (`team_week_id` unique) | Team's-perspective weekly result with opponent resolved: `opponent_owner_id`/`opponent_team_name`/`opponent_owner_name`, `score_for`/`score_against`, `outcome`, `margin`, `is_clutch`, `is_shotgun`, `is_playoff`. Backs `season_schedule` + `int__owner_head_to_head`. A singular test (`assert_league_reg_season_zero_sum`) asserts the opponent self-join nets to zero league-wide each season (wins=losses, PF=PA). | `stg__team_weeks` (self-join), `int__owner_team_year_map`, `int__matchup_week_playoff_map`, `int__shotguns` |
| `int__team_week_highlights` | team-week (regular season, `team_week_id` unique) | A boolean per **week-grain** league highlight: `is_shotgun`, `is_lucky_win`/`is_unlucky_loss`, `is_clutch_win`/`is_clutch_loss`, `is_best_week`/`is_worst_week` (held the league single highest/lowest score that season → aligns with `matchup_title`/`bad_matchup_title`), `is_tightest_game`/`is_biggest_blowout` (won the season's single smallest/largest margin → aligns with `tightest_matchups`/`biggest_blowouts`). Each flag is the week-level occurrence of a season highlight; reads the league single-week extremes from the shared `int__league_season_week_extremes` (same source as the matching season titles, so chip↔title can't drift). Backs `season_schedule`; intended source for a future activity/notification feed (FF-010). | `int__team_week_results`, `int__lucky_records`, `int__league_season_week_extremes` |
| `int__team_season_summary` | team-season (`team_year_id` unique) | Owner-spotlight card: record, acquisitions, FAAB budget, standing, trades, streak, clutch record, shotguns, regular-season points for/against + per-week. Owner is the actual per-season owner; points for/against from `int__owner_season_scoring`, clutch record from `int__clutch_records`. Backs `season_overview`. | `base_s001__teams`, `int__owner_season_scoring`, `int__clutch_records`, `int__owner_team_year_map`, `int__weeks_played_by_year`, `int__team_shotgun_counts` |

## Marts (`main_marts`) — page-specific final tables

Directory mirrors the frontend page that consumes it. Owner name attached, decimals rounded
for display.

| Mart | Frontend page | Purpose |
|---|---|---|
| `splash/current_standings` | `/` | Current-season standings table. |
| `splash/current_shotgun_counter` | `/` | Current-season shotgun tally per owner. |
| `owners/home/owners_by_year` | `/owner_history` | Owner grid options grouped by year. **Played seasons only** (gated by `int__weeks_played_by_year`) — the upcoming drafted-but-unplayed season has no history to render. |
| `owners/spotlight/season_overview` | `/owner_history/{owner}/{year}` | Season summary card for an owner. |
| `owners/spotlight/season_schedule` | `/owner_history/{owner}/{year}` | **Thin** mart: one row per owner-week (reg season) = display columns (opponent, scores, `outcome`) joined to the boolean week-grain highlight flags from **`int__team_week_highlights`** (all the "what happened" logic lives there). Frontend renders the flags as section-colored chips. Upstream: `int__team_week_results`, `int__team_week_highlights`. |
| `roster_production/owner_roster_production` | `/owner_history/{owner}/{year}` | One row per player an owner rostered that regular season, ordered by `points_started` desc. Surfaces `points_held`/`points_started`/`weeks_*`/`avg_points`/`player_reg_season_points` (player's reg-season total across all owners) plus two distinct rates: `capture_rate` = `points_started / player_reg_season_points` (share of the player's *whole season* started — low when only rostered part of the year) and `roster_utilization` = `points_started / points_held` (of points scored *while on this roster*, the share started — pure start/sit deployment, ~100% for a full-time starter). Both null for non-positive denominators / net-negative starts. Backs the **`/roster_production`** page (Total + Capture % + Roster Util %). Upstream: `int__owner_player_season`, `int__player_season_stats`. |
| `owners/spotlight/owner_weekly_lineup` | `/owner_history/{owner}/{year}` | One row per owner-(NFL week)-player (reg season **+ meaningful postseason games**): `is_started`/`is_optimal` + `actual_slot`/`optimal_slot` + points + `matchup_week` + `week` (NFL) + `is_playoff`. Backs the spotlight **weekly Roster tab**, keyed by `matchup_week` so a two-week (2018-2019) playoff matchup renders both NFL-week lineups. Thin wrap of `int__optimal_lineup_players`. |
| `owners/spotlight/postseason_schedule` | `/owner_history/{owner}/{year}` | One row per **meaningful** postseason game an owner played (winners-bracket games + 3rd-place + toilet-bowl final), with opponent owner, round label, `week` (links to the Roster tab week select), and both scores (opponent score via a self-join on year+round). Backs the spotlight Postseason Schedule tab; non-bracket owner-seasons have no rows (empty state). Playoff scores are shown as schedule only, never mixed into metrics. Upstream: `int__postseason_team_weeks`, `int__owner_team_year_map`. |
| `player_data/player_data_table` | `/player_data` | Aggregated player scoring table. |
| `strength_of_schedule/strength_of_schedule` | `/strength_of_schedule` | SOS leaderboard. |
| `draft_analysis/snake_draft_table` | `/draft_analysis` | Snake-draft board. |
| `draft_analysis/auction_draft_table` | `/draft_analysis` | Auction-draft board. |
| `league_highlights/all_time_records` | `/league_highlights` | One row per metric x ranked owner (tidy/long): tennis-style title counts, career totals, single-extreme records. Counts/totals read `int__owner_career_summary`; records read the season/week/game intermediates. Metric metadata (key→section/category/label/sort_sign/result_n/value_format/**description**/**subtitle_kind**/**display_order**) comes from the **`all_time_record_metrics` seed**; both leaderboards and records keep top 3 (`result_n`). **Ranking uses SQL `rank()` on purpose (standings-style, not `dense_rank`):** tied owners share a place and the next *distinct* value skips ahead (a 7-way tie for 1st → next is rank 8, not rank 2), and `metric_rank <= result_n` keeps the top *places*. So a tie wider than `result_n` at the top legitimately fills the card with co-leaders and shows no 2nd/3rd — exactly like a real podium (e.g. 7 owners tied at 4 playoff appearances render as seven 1st-places; the 3-appearance owners are not shown because nobody is in 2nd). This is intended — mirror real standings; only reach for `dense_rank` if a specific metric should always surface the next distinct tier (none currently do). A per-metric `tiebreak` (the secondary sort in the `ranked` CTE) can break ties when a true 1/2/3 is wanted. `description` = tooltip text; `subtitle_kind` drives the card subtitle — `years` (season-award counts surface **`years_won`**, a chrono `string_agg` of title years from `int__season_titles_long` — and for `snub_total_count` / `luck_in_total_count`, the snub/lucky-in years from `int__season_titles`), `tenure` (career accumulations → UI shows seasons-played), `context` (single records → year/week/opponent detail); `display_order` = reading order within a section (single week → season → career → titles), drives card + sub-cluster order in the UI; `detail` = secondary line behind the value (clutch W-L record for clutch-% rows, losing owner + winner-loser score for margin records, the English label + points-for tiebreak for `biggest_snub`/`biggest_luck_in`, e.g. "playoff teams outscored · 1867 PF"). The Playoff category carries the full snub/lucky-in family: title (tennis), `*_total_count` (career occurrences via `is_snubbed`/`is_lucked_in`), `biggest_snub`/`biggest_luck_in` (record ranked by teams out/under-scored), plus the existing points records. **`metric_type` taxonomy** (when adding a seed row): `record` = the single most-extreme value ever, one holder; `count` = tennis-style tally of *how many seasons* an owner held a title (magnitude within a season is irrelevant — winning by 0.1 or 400 both count as 1); `total` = a continuous career sum, where "most …" and "least …" are the same metric sorted desc vs asc. The **Matchups** section's `Combined` category (`highest_shootouts`/`lowest_slugfests`, top-3 `record`) ranks games by total combined score from `int__matchup_margins.combined`, mirroring the `Margins` records. The **Postseason** section (mirrors the H2H dashboard's) holds a `Results` category — `most_championships`/`most_last_place_finishes`/`most_runner_ups`/`most_playoff_appearances`/`most_toilet_bowls` plus the two W-L leaderboards `career_playoff_record` (championship bracket) and `career_toilet_bowl_record`, ranked by total wins with win % as the tiebreak, the W-L record headlined (override) and win % as the subtitle — all from `int__owner_postseason_summary` (championship years feed the `years` subtitle via a `title_years` union on `int__team_postseason`) — plus the `Playoff` category (snub/lucky-in family, moved here from `Scoring`). The **Transactions** section (`most_acquisitions_career`/`most_trades_career`, `count` type so zero-trade owners drop off) ranks career roster moves from `int__owner_career_summary`. The **Lineups** section holds the title counts for both lineup-setter pairs (`best_lineup_title_count`/`worst_lineup_title_count` + `most_efficient_lineup_title_count`/`least_efficient_lineup_title_count`), the single-season records `most_points_left_season`/`fewest_points_left_season` (from `int__season_titles.points_left_on_table`; subtitle = efficiency) and `highest_lineup_efficiency_season`/`lowest_lineup_efficiency_season` (subtitle = points left), plus `most_underutilized_player` (the biggest single player-season bench waste ever, from `int__owner_player_underutilization`; subtitle = the player + season). `display_value` formatting is shared via the `format_metric_value(value, format)` macro (`macros/`), wrapped in a `coalesce` with each mart's metric-specific overrides. |
| `league_highlights/season_highlights` | `/league_highlights` | One row per `year` x `metric_key` x `rank`. Title holders (co-titles included) read straight from `int__season_titles_long where is_title_holder`; the single tightest game / biggest blowout come from `int__matchup_margins` as single-winner **Matchups** titles, same as every other season crown — there's no separate top-3 leaderboard block — with the full `detail` subtitle composed in the mart (`def. {opponent} · {score} · Wk N`), so the frontend renders it as-is. **Every By-Season metric is a single-winner title** (rank 1, co-titles shared), so the old `metric_type` / `result_n` columns were dropped from the seed + mart. Metric metadata + `description` (tooltip) + **`section`** (Scoring/Matchups/Shotgun/Clutch grouping) + **`display_order`** (intra-section card order) come from the **`season_highlight_metrics` seed**; the frontend clusters title cards by `section` mirroring the All-Time tab. Each title row also gets a **`detail` subtitle** describing how/when/where it was earned — end-of-reg-season seed (`standing`, e.g. "7th seed") for scoring/bottom-scorer/snub/lucky-in, the key week(s) (from `int__shotguns`/`int__lucky_records`/`int__team_week_results`) for week/shotgun/lucky/unlucky titles — and clutch titles headline the W-L `record` (`int__clutch_records`) as their `display_value`. The seed also carries `empty_label` (e.g. "Everyone shotgunned this season"); the frontend reads the catalog directly so a title with no holder in a year still shows a placeholder card. The **Transactions** section adds per-season `most_acquisitions_season`/`most_trades_season` titles (from `base_s001__teams` via `int__owner_team_year_map`), with zero-count seasons dropped so a quiet year shows the empty-state card. The **Combined** category (in Matchups) adds `highest_shootouts`/`lowest_slugfests` titles — the season's single highest/lowest combined-score game from `int__matchup_margins.combined`. The **Postseason** section adds `champion`/`toilet_bowl_loser` titles (from `int__team_postseason`, displayed with the owner's regular-season `seed` via the `seed` value-format — champion = upset factor, toilet bowl = collapse) plus the snub/lucky-in titles moved here from `Scoring`. The **Lineups** section adds the two lineup-setter pairs — `best_lineup_title`/`worst_lineup_title` (fewest/most reg-season points left on the bench; value = points left, subtitle = efficiency) and `most_efficient_lineup_title`/`least_efficient_lineup_title` (highest/lowest share of the optimal captured; value = efficiency %, subtitle = points left), both from `int__owner_lineup_efficiency` — plus `underutilized_player` (the best player most left on the bench that season; value = bench points, subtitle = player + weeks benched, from `int__owner_player_underutilization`). |
| `h2h_dashboard/h2h_owner_metrics` | `/h2h_dashboard` | One row per owner × comparable metric (tidy/long, every owner kept — no ranking). `metric_value` drives the leader highlight (`max(metric_value * sort_sign)` per row); `display_value` is the formatted string (`override_display` for record/ordinal metrics, else formatted by `value_format`). Catalog metadata (section/label/format/sort/order/description) comes from the **`h2h_metrics` seed** (~45 metrics across Record/Scoring/Postseason/Matchups/Shotgun/Clutch/Lineups/Transactions; the Postseason section keeps `playoff_game_record` = winners-bracket only and a separate `toilet_bowl_record`; the Lineups section adds `best_lineup_titles`/`worst_lineup_titles` + `most_efficient_lineup_titles`/`least_efficient_lineup_titles` counts). Values compose `int__owner_career_summary` (career totals + title counts + `acquisitions_total`/`trades_total` for the Transactions section), `int__owner_postseason_summary` (postseason), `int__owner_season_scoring` (PF/PA + highest season), `int__team_week_results` (`week_extremes` CTE — best/worst week + their when), `int__matchup_margins` (biggest blowout + that game's matchup score), and the career record summed from `int__owner_head_to_head`. Single-record metrics carry **context in parens** via `override_display` (`highest_scoring_season` → `2052.84 (2023)`, `best_week_ever`/`worst_week_ever` → `200.5 (2023 W4)`, `biggest_blowout` → `107.22 (200.5-93.28)`). Adding a stat = one seed row + one candidate branch. |
| `h2h_dashboard/h2h_matchup_records` | `/h2h_dashboard` | One row per owner pair — the rivalry formatted for the two H2H sections, in two field flavors. **Per-owner** (each side's own number): `avg_points`, `longest_win_streak`, `biggest_win`, `held_under_100`, `lucky_wins` (won below the week median vs this opp), `unlucky_losses` (lost above the week median to this opp). **Owner-perspective `*` + `*_leader`** (this row's owner's value first + leader owner_id, null when even; the page reads the left owner's row so the value orders left→right by column and the arrow points at the leader): `rivalry_record` (record + win%), `rivalry_playoff_record` (championship-bracket series, `None` when they never met there), `rivalry_toilet_record` (toilet-bowl series, `None` when they never met there), `rivalry_points`, `rivalry_avg_margin`, `rivalry_streak`, `rivalry_last_meeting`, `rivalry_clutch`, plus the game-extremes `closest_game`/`highest_shootout`/`lowest_slugfest` whose `*_leader` is that game's **winner**. Single-game extremes carry the signed margin (`159.2-108.6 (+50.6) (2022 W3)`); shootout/slugfest carry the matchup score (`296 (159.7-136.2) (2022 W14)`). Thin display wrap of `int__owner_head_to_head`. |
| `postseason_history/postseason_timeline` | `/postseason_history` | One row per `year`: the four headline finishers (champion / runner-up / 3rd / toilet-bowl loser = dead last), each with `owner_name` + reg-season `seed` (upset/collapse framing). Filtered aggregates over `int__team_postseason` × `int__owner_team_year_map` (exactly one team carries each placement flag per season). |
| `postseason_history/postseason_trophy_case` | `/postseason_history` | One row per `owner_id`: career postseason hardware (championships/runner_ups/third_places/toilet_bowl_appearances/last_place_finishes/best_finish) + the two **separate** records — playoff W-L (winners bracket, reconciles with apps) and toilet-bowl W-L — plus `last_year` (the owner's most recent season, so each card links to their owner-spotlight). Thin display wrap of `int__owner_postseason_summary` × tenure (max year from `int__owner_team_year_map`), ordered most-decorated first. |
| `postseason_history/postseason_snub_luck` | `/postseason_history` | One row per **snubbed or lucked-in team-season** (`kind` ∈ {snub, lucky_in}), powering the side-by-side Snubs / Lucky-In lists on the brackets tab. Uses the **platform-canonical** definitions from `int__season_titles` (`is_snubbed` = missed playoffs despite outscoring ≥1 playoff team; `is_lucked_in` = made playoffs despite being outscored by ≥1 non-playoff team) so it stays consistent with League Highlights / H2H. Carries `context_count` (teams out/under-scored, for ordering) + a pre-composed `detail` subtitle. Upstream: `int__season_titles`. |
| `postseason_history/postseason_bracket` | `/postseason_history` | One row per winners/toilet-bowl **bracket slot per round** — a game *or* a first-round bye (`is_bye`). Collapses the two team-perspective rows of each meaningful postseason game (`int__postseason_team_weeks`, filtered to `is_meaningful` = winners ladder + 3rd-place game + toilet-bowl final) into one matchup anchored on the better (lower) `top_seed`; carries both teams' owner/seed/score + `winner_owner_id` (higher score) + `advancer_owner_id` (who moves on — the winner in the championship bracket, but the **loser** in the toilet bowl, where you advance toward dead last by losing). **Byes are data-derived** (winners team absent from round 1 → lone `top_seed_*` slot, null opponent), so the 4-team (no byes) and 6-team (top-2 bye) eras both work. `round_label` (First Round/Semifinal/Championship/3rd Place Game/Toilet Bowl) is computed **relative to each bracket's last round**, not hard-coded weeks, so it's format-agnostic. Upstream: `int__postseason_team_weeks`, `int__owner_team_year_map`. |
| `utilities/current_year` | frontend `utils.py` | Single row, latest season `year` (dropdown default). Wraps `int__current_season_year`. |
| `utilities/owner_year_map` | frontend `utils.py` | One row per owner-season (`owner_id`, `owner_name`, `year`) for owner/year dropdowns + tabs. **Played seasons only** (`int__owner_team_year_map` gated by `int__weeks_played_by_year`) so navigation never offers a drafted-but-unplayed season the spotlight can't render. |
| `utilities/owners` | frontend (league highlights) | **Owner dimension** — one row per `owner_id`: `owner_name`, `first_year`, `last_year`, `seasons_played` (tenure), `is_active` (true = fielded a team in the current season; false = retired). Wraps `int__owner_team_year_map` × `int__current_season_year`. |
| `utilities/nfl_teams` | frontend `utils.py` | Distinct `nfl_team` list (incl. `'None'` sentinel) for the NFL-team filter. Wraps `base_s001__players`. |

---

## Gotchas / non-obvious data facts

- **`is_playoff` includes consolation games.** In `base_s001__matchups`, `is_playoff` is true
  for `WINNERS_BRACKET`, `WINNERS_CONSOLATION_LADDER`, **and** `LOSERS_CONSOLATION_LADDER` —
  so every team "appears in a playoff matchup." It really means "is a postseason week" (used
  for the regular-season filter `not is_playoff` in `int__matchup_week_playoff_map`). For
  bracket/placement logic, use `int__team_postseason` / `int__postseason_team_weeks` instead.
- **2018 is special-cased upstream** in the source `MatchupTransformer` (uses `scoreboard`
  instead of `box_scores`). Season length also differs: 13 regular-season games in 2018–2019
  vs 14 from 2021 on — relevant whenever comparing season totals across years (prefer
  per-game where fairness matters).
- **Within a season every owner plays the same number of regular-season games**, so total
  points and points-per-game produce identical *within-season* rankings; they only diverge
  *across* seasons (the 13- vs 14-game difference).
- **`weeks_raw` (teams) vs `matchup_lineup` (matchups)** are two different lineup sources;
  `stg__team_weeks` flattens the former, `stg__matchup_players` the latter.
- **`away_team` can be null** in `base_s001__matchups` (bye week opponent) — the home/away
  union filters `away_team is not null`.
- **Everything is `materialized: table` with `contract: enforced`.** A new/renamed column
  fails the build unless its `data_type` is declared in the sibling `properties.yml`.

### Postseason / bracket rules (read before touching `int__*postseason*`)

Regular season and postseason are **separate concepts** — game counts per team are *not*
consistent in the postseason because they depend on outcomes. All current regular-season
metrics exclude the postseason entirely (`not is_playoff`). Postseason logic lives in
`int__postseason_team_weeks` (team-week) and `int__team_postseason` (team-season).

At the end of the regular season each team is in one of three brackets:

- **Winners bracket** (championship) — `playoff_team_count` teams (4 pre-2020, 6 since).
  Single elimination. **Determined by actual `WINNERS_BRACKET` participation, not seed**
  (in 2018 the league ran two divisions — East/West, since removed — so `standing` is
  division-aware and ≠ playoff seed: the winners bracket is standings 1,3,4,5 because a 7-6
  West division winner was seeded 2nd). 1st = wins the final, 2nd = loses it, 3rd = wins the
  game between the two semifinal losers.
- **Toilet bowl** — the **bottom 4 seeds** (`standing > team_count - 4`), 2 weeks. Round 1:
  the two winners **escape** (a win is good). Round 2: the two round-1 losers play — that
  loser is **last (12th)**, the winner is **2nd-to-last (11th)**. The two escapees' round-2
  games are meaningless (they play middle teams).
- **Kiss my sister** (middle) — everyone else. **Every one of their games is meaningless.**

`is_meaningful` (in `int__postseason_team_weeks`) is the flag that protects metrics: true only
for winners-bracket games while alive, the 3rd-place game, and real toilet-bowl games.
Everything else (eliminated-team placement ladders, escapee filler, all middle games) is false.

Format quirks to respect:
- **2-week format (2018-19)** labels the 3rd-place game `WINNERS_BRACKET` (no consolation
  ladder); the **3-week format (2020+)** drops eliminated teams to `WINNERS_CONSOLATION_LADDER`.
  So championship/3rd-place games are identified by **semifinal result** (second-to-last
  postseason week), not by the final week's `matchup_type`.
- **ESPN `final_standing` is reliable for 1st/2nd/3rd** (validated by
  `tests/assert_postseason_placements_match_espn.sql`) but **wrong for the toilet bowl** — it
  runs its own consolation ladder over all bottom teams, so the league's bottom-4 toilet
  outcome is reconstructed from bracket progression instead.
